{
  "name": "cubo-explorador",
  "version": "1.0.0",
  "private": true,
  "description": "Cubo Explorador — jogo 3D com React e Three.js (plataformas geradas dinamicamente, árvores, partículas, fade-out).",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html --public-url ./"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "three": "^0.156.0"
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  }
}